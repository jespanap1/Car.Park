<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Park</title>
</head>
<body>
    <h1>Estado del Parqueadero</h1>
    <% if (typeof distanceFromArduino !== 'undefined') { %>
        <p>Distancia del Arduino: <%= distanceFromArduino %> cm</p>
    <% } else { %>
        <p>Distancia del Arduino no disponible</p>
    <% } %>
</body>
</html>
